# MultiRag_Project - 설치된 패키지 목록

## 가상환경 정보
- **경로**: `/Users/sunmiyang/Desktop/MultiRag_Project/venv`
- **Python 버전**: Python 3.x
- **설치 일자**: 2026-05-13

---

## 핵심 RAG 프레임워크

| 패키지 | 버전 | 설명 |
|--------|------|------|
| `langchain` | 0.3.30 | RAG 파이프라인 구성을 위한 핵심 오케스트레이션 프레임워크 |
| `langchain-community` | 0.3.31 | LangChain 커뮤니티 통합 패키지 (다양한 문서 로더, 벡터스토어 등) |
| `langchain-openai` | 0.3.35 | LangChain과 OpenAI 모델 연동 패키지 |
| `langchain-core` | 0.3.86 | LangChain 핵심 추상화 및 인터페이스 |
| `langchain-text-splitters` | 0.3.11 | 문서를 청크(chunk)로 분할하는 텍스트 스플리터 |

---

## LLM 및 AI 모델

| 패키지 | 버전 | 설명 |
|--------|------|------|
| `openai` | 2.36.0 | OpenAI API 클라이언트 (GPT-4, GPT-3.5 등) |
| `sentence-transformers` | 5.1.2 | 로컬 임베딩 모델 (문장을 벡터로 변환) |
| `transformers` | 4.57.6 | Hugging Face 트랜스포머 모델 라이브러리 |
| `torch` | 2.8.0 | PyTorch 딥러닝 프레임워크 |
| `huggingface-hub` | 0.36.2 | Hugging Face 모델 허브 연동 |
| `tokenizers` | 0.22.2 | 고속 토크나이저 |
| `tiktoken` | 0.12.0 | OpenAI 모델용 토큰 카운터 |
| `safetensors` | 0.7.0 | 안전한 모델 가중치 저장 포맷 |

---

## 벡터 데이터베이스

| 패키지 | 버전 | 설명 |
|--------|------|------|
| `chromadb` | 1.5.9 | 영구 저장이 가능한 벡터 데이터베이스 |
| `faiss-cpu` | 1.13.0 | Facebook AI의 고속 유사도 검색 라이브러리 (인메모리) |

---

## 문서 처리 및 로딩

| 패키지 | 버전 | 설명 |
|--------|------|------|
| `pypdf` | 6.11.0 | PDF 문서 파싱 및 텍스트 추출 |
| `unstructured` | 0.18.3 | Word, HTML, 이메일 등 다양한 포맷 문서 파싱 |
| `beautifulsoup4` | 4.14.3 | HTML/XML 파싱 라이브러리 |
| `lxml` | 6.1.0 | XML/HTML 고속 파싱 |
| `html5lib` | 1.1 | HTML5 파서 |
| `python-magic` | 0.4.27 | 파일 형식 자동 감지 |
| `filetype` | 1.2.0 | 파일 타입 감지 |
| `chardet` | 5.2.0 | 문자 인코딩 자동 감지 |

---

## 데이터 처리 및 분석

| 패키지 | 버전 | 설명 |
|--------|------|------|
| `numpy` | 2.0.2 | 수치 연산 및 배열 처리 |
| `pandas` | 2.3.3 | 데이터프레임 기반 데이터 분석 |
| `scipy` | 1.13.1 | 과학 연산 라이브러리 |
| `scikit-learn` | 1.6.1 | 머신러닝 알고리즘 (유사도 계산 등) |

---

## 환경 설정 및 유틸리티

| 패키지 | 버전 | 설명 |
|--------|------|------|
| `python-dotenv` | 1.2.1 | `.env` 파일에서 API 키 등 환경변수 로드 |
| `pydantic` | 2.13.4 | 데이터 유효성 검사 및 설정 관리 |
| `pydantic-settings` | 2.11.0 | Pydantic 기반 설정 관리 |
| `tqdm` | 4.67.3 | 진행 상태 표시 프로그레스바 |
| `rich` | 15.0.0 | 터미널 출력 서식 지정 (컬러, 표 등) |
| `requests` | 2.32.5 | HTTP 요청 라이브러리 |
| `httpx` | 0.28.1 | 비동기 HTTP 클라이언트 |

---

## 자연어 처리

| 패키지 | 버전 | 설명 |
|--------|------|------|
| `nltk` | 3.9.2 | 자연어 처리 툴킷 (토큰화, 형태소 분석 등) |
| `langdetect` | 1.0.9 | 텍스트 언어 자동 감지 |
| `emoji` | 2.15.0 | 이모지 처리 |
| `rapidfuzz` | 3.13.0 | 고속 문자열 유사도 비교 |

---

## 가상환경 활성화 방법

```bash
source /Users/sunmiyang/Desktop/MultiRag_Project/venv/bin/activate
```

## 패키지 재설치 방법

```bash
pip install langchain langchain-community langchain-openai openai chromadb faiss-cpu \
    sentence-transformers pypdf tiktoken python-dotenv numpy pandas unstructured
```
